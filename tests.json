{
  "mergeid_mustache_merge":{
    "mergeid": "{{mergeid_merge_value}}"
  },
  "recursion_test":{
    "madeit":true,
    "do_it_again": {
      "mergeid": "recursion_test2"
    }
  },
  "extend_test" : {
    "contains" : "something"
  },
  "recursion_test2":{
    "madeit2":true
  },
  "merge_with_predefined_options": {
    "mergeid": {
      "id": "favorite_action"
    },
    "options": {
      "_readonly": true,
      "_readonly_comment": "leave out or set to true if you want the checkbox to be ready-only",
      "checked": "star",
      "unchecked": "star-outline"
    }
  },
  "merge_with_override": {
    "type": "action",
    "actionType": "event",
    "options": {
      "target": "{{column_star_target}}",
      "params": {
        "type": "action",
        "actionType": "ajax",
        "options": {
          "target": {
            "uri": "{{column_target_uri}}",
            "options": {
              "type": "POST",
              "data": "{{id}}"
            }
          }
        }
      }
    }
  },
  "simple_merge": {
    "action": {
      "options": {
        "target": "{{column_star_target}}"
      }
    }
  }
}
